# python3 -m pip install --upgrade pip
# python3 -m pip install --upgrade package_name

# python3 -m pip install --user package_name
# python3 -m pip install --user pytest

# Most commonly used assertion commands in tests:
'''
Assertion:	                Description:
------------------------------------------------------------------------
assert a == b	            Assumes that two values are equal.
assert a != b	            Assumes that two values are not equal.
assert a	                Assumes that a evaluates to True.
assert not a	            Assumes that a evaluates to False.
assert element in list	    Assumes that the element is in the list.
assert element not in list	Assumes that the element is not in the list.
'''
